=============================
dpkg = аналог rpm: низкоуровневая утилита для установки/удаления/запроса отдельных .deb файлов. 
=============================
dpkg -i
dpkg -r
dpkg -P
# Просмотреть содержимое локального пакета
dpkg -c <имя_пакета>*.deb
# Просмотреть содержимое установленного пакета
dpkg -L
dpkg -S
dpkg --status
dpkg --contents
# получить список всех пакетов в системе
dpkg --list
=============================
apt (Advanced Package Tool) = аналог dnf/yum: высокоуровневая система управления репозиториями и зависимостями.
=============================
apt update (обновить кеш индексов = dnf makecache)
apt upgrade / apt full-upgrade (обновить пакеты, аналог dnf upgrade)
apt install
# удалить только двоичные файлы пакета
apt remove
# полностью удалить пакет вместе с его конфигурационными файлами
apt remove --purge 
apt purge
apt autoremove
apt search
apt show
# просматривать содержимое любого пакета в репозиториях без предварительной загрузки
apt-file list curl
apt list --installed / --upgradeable
-----------------------------
apt-get & apt-cache: "Старая" основа apt. Знайте их, но фокусируйтесь на apt. apt — более дружелюбная оболочка.
-----------------------------
aptitude: Альтернативный TUI/CLI фронтенд к APT (менее распространен сейчас, но знать о его существовании полезно).
-----------------------------
shutdown -r now — немедленная перезагрузка
shutdown -r +5 — перезагрузка через 5 минут
systemctl reboot
-----------------------------
