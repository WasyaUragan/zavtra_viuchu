# del all lines with "pattern"
:g/pattern/d
# select smth in visual mode and dell
  CTRL + V 
  select 
  press D
# poisk bez ucheta regictra
  /^pattern
# search line ends with
  /pattern$
# отсортировать по алфавиту
  :sort

